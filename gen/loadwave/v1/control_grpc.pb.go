// Copyright the LoadWave Authors.
// SPDX-License-Identifier: AGPL-3.0-or-later

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: loadwave/v1/control.proto

package loadwavev1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ControlService_Join_FullMethodName = "/loadwave.v1.ControlService/Join"
)

// ControlServiceClient is the client API for ControlService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ControlService is the single RPC that ties the cluster together.
//
// The node always dials its supervisor and holds one long-lived bidirectional
// stream open. Commands travel down it and telemetry travels up it. Because
// the connection is outbound from the node, agents work from behind NAT and
// through egress-only firewalls with no inbound ports, and a broken stream is
// an unambiguous signal that the node is gone.
//
// The same service is used at both tiers: agents speak it to the coordinator
// over TCP, and worker processes speak it to their local agent over a Unix
// domain socket.
type ControlServiceClient interface {
	// The envelope names describe direction rather than the RPC, because the
	// same pair is reused for the agent tier and the worker tier. JoinRequest /
	// JoinResponse would obscure that symmetry.
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	Join(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[NodeUp, NodeDown], error)
}

type controlServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewControlServiceClient(cc grpc.ClientConnInterface) ControlServiceClient {
	return &controlServiceClient{cc}
}

func (c *controlServiceClient) Join(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[NodeUp, NodeDown], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &ControlService_ServiceDesc.Streams[0], ControlService_Join_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[NodeUp, NodeDown]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ControlService_JoinClient = grpc.BidiStreamingClient[NodeUp, NodeDown]

// ControlServiceServer is the server API for ControlService service.
// All implementations must embed UnimplementedControlServiceServer
// for forward compatibility.
//
// ControlService is the single RPC that ties the cluster together.
//
// The node always dials its supervisor and holds one long-lived bidirectional
// stream open. Commands travel down it and telemetry travels up it. Because
// the connection is outbound from the node, agents work from behind NAT and
// through egress-only firewalls with no inbound ports, and a broken stream is
// an unambiguous signal that the node is gone.
//
// The same service is used at both tiers: agents speak it to the coordinator
// over TCP, and worker processes speak it to their local agent over a Unix
// domain socket.
type ControlServiceServer interface {
	// The envelope names describe direction rather than the RPC, because the
	// same pair is reused for the agent tier and the worker tier. JoinRequest /
	// JoinResponse would obscure that symmetry.
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	Join(grpc.BidiStreamingServer[NodeUp, NodeDown]) error
	mustEmbedUnimplementedControlServiceServer()
}

// UnimplementedControlServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedControlServiceServer struct{}

func (UnimplementedControlServiceServer) Join(grpc.BidiStreamingServer[NodeUp, NodeDown]) error {
	return status.Error(codes.Unimplemented, "method Join not implemented")
}
func (UnimplementedControlServiceServer) mustEmbedUnimplementedControlServiceServer() {}
func (UnimplementedControlServiceServer) testEmbeddedByValue()                        {}

// UnsafeControlServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ControlServiceServer will
// result in compilation errors.
type UnsafeControlServiceServer interface {
	mustEmbedUnimplementedControlServiceServer()
}

func RegisterControlServiceServer(s grpc.ServiceRegistrar, srv ControlServiceServer) {
	// If the following call panics, it indicates UnimplementedControlServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ControlService_ServiceDesc, srv)
}

func _ControlService_Join_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(ControlServiceServer).Join(&grpc.GenericServerStream[NodeUp, NodeDown]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ControlService_JoinServer = grpc.BidiStreamingServer[NodeUp, NodeDown]

// ControlService_ServiceDesc is the grpc.ServiceDesc for ControlService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ControlService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "loadwave.v1.ControlService",
	HandlerType: (*ControlServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Join",
			Handler:       _ControlService_Join_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "loadwave/v1/control.proto",
}
